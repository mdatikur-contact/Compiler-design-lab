#include <stdio.h> // Include the standard input/output library



int main() {
    int num1 = 10; 
    int num2 = 20; 
    int sum;      

    sum = num1 + num2;

   s a format specifier for integers.
     */
    printf("The sum of %d and %d is: %d\n", num1, num2, sum);

    return 0; 
}